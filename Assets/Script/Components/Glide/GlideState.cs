namespace Game.Components.Glide
{
    /// <summary>Whether the companion is following (normal) or the player is gliding.</summary>
    public enum GlideState
    {
        Following = 0,
        Gliding = 1
    }
}
